# Models Directory

Place your `.onnx` models in this directory.

For the Magic Eraser to use true AI inpainting, drop a LaMA ONNX model here named exactly:
`lama.onnx`

If the model is missing, the application will gracefully fall back to a content-aware blur filter.
